services:
  poliplanner:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      # Keep the SQLite database outside the image and across container updates.
      - poliplanner-data:/app/db

volumes:
  poliplanner-data:
    name: poliplanner-data
